# app/db/auto_migrate.py

# Auto-migrate behavior was removed per your request.
# Keeping a stub in case you want to re-enable custom migration logic later.

def run_auto_migrations():
    print("auto_migrate is disabled. Use SQLModel.metadata.create_all(engine) on startup instead.")
